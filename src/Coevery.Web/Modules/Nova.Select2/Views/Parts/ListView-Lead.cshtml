@using Coevery.DisplayManagement.Shapes
@{
    var gridId = "leadGrid";
    var listViewModel = new {
        GridId = gridId
    };
    Shape actionShape = New.Parts_ListViewActions(GridId: gridId);
    actionShape.Metadata.Alternates.Add("Parts_ListViewActions__Lead");

    Shape viewShape = New.Parts_ListViewGrid(listViewModel);
    viewShape.Metadata.Alternates.Add("Parts_ListViewGrid__Lead");
}

@Display(actionShape)
@Display(viewShape)
@{
    string displayName = "收费项目标准";
    Layout.Title = displayName;
    Layout.TitleDescription = string.Format(T("编辑一个{0}").Text, displayName);
}
@if (Model.Actions != null) {
    @Display(Model.Actions)
}
@Display(Model.Content)
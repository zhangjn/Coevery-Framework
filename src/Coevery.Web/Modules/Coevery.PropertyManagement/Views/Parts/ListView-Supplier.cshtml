@using Coevery.DisplayManagement.Shapes
@{
    var gridId = "supplierGrid";
    var listViewModel = new {
        GridId = gridId
    };
    Shape actionShape = New.Parts_ListViewActions(GridId: gridId);
    actionShape.Metadata.Alternates.Add("Parts_ListViewActions__Supplier");

    Shape viewShape = New.Parts_ListViewGrid(listViewModel);
    viewShape.Metadata.Alternates.Add("Parts_ListViewGrid__Supplier");
}

@Display(actionShape)
@Display(viewShape)
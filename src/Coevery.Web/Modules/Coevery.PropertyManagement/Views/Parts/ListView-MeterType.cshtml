@using Coevery.DisplayManagement.Shapes
@{
    var gridId = "metertypeGrid";
    var listViewModel = new {
        GridId = gridId
    };
    Shape actionShape = New.Parts_ListViewActions(GridId: gridId, ContentItem: Model.ContentItem);
    actionShape.Metadata.Alternates.Add("Parts_ListViewActions__MeterType");

    Shape viewShape = New.Parts_ListViewGrid(listViewModel);
    viewShape.Metadata.Alternates.Add("Parts_ListViewGrid__MeterType");
}

@Display(actionShape)
@Display(viewShape)